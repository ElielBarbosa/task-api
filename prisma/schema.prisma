generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model tb_tarefa {
  id_tarefa        Int     @id @default(autoincrement())
  titulo_tarefa    String  @db.VarChar(255)
  descricao_tarefa String? @db.VarChar(255)
}
